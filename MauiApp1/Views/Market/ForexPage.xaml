<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.Market.ForexPage"
             Title="ForexPage">
    <ScrollView>
        <Grid Margin="20,35,20,20" BackgroundColor="{StaticResource Gray200}">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0"  Text="By default, a Grid contains one row and one column." BackgroundColor="{StaticResource Gray400}" />
            <Label Grid.Row="1" Grid.Column="1" Text="By default, a Grid contains one row and one column." BackgroundColor="{StaticResource Gray400}" />
            <Label Grid.Row="1" Grid.Column="2" Text="By default, a Grid contains one row and one column." BackgroundColor="{StaticResource Gray400}" />
        </Grid>
    </ScrollView>
</ContentPage>